import { RippleButton } from '@/components/ui/ripple-button'

/**
 * A demo component for a button with a ripple effect.
 *
 * @returns {React.JSX.Element} The rendered ButtonRippleEffectDemo component.
 */
const ButtonRippleEffectDemo = () => {
  return <RippleButton>Ripple Effect</RippleButton>;
}

export default ButtonRippleEffectDemo
